{
    "serverInfo": {
        "name": "AI Answer Copier — Markdown Formatter",
        "version": "2.3.0"
    },
    "authentication": {
        "required": false
    },
    "tools": [
        {
            "name": "harmonize_markdown",
            "description": "Standardize and normalize Markdown syntax without changing the document's meaning. Re-formats headers (ATX-style), normalizes list markers to '-', enforces fenced code blocks with backticks, and applies consistent indentation. Side effects: when output_path is provided, writes the harmonized Markdown to disk. When output_path is omitted, returns the harmonized text as a string with no file I/O.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert. Supports GitHub-Flavored Markdown (tables, task lists, strikethrough) and KaTeX math expressions. Pass the full document content as a string, not a file path." },
                    "output_path": { "type": "string", "description": "Optional. Absolute or relative file path where the result will be saved. Parent directories are created automatically. If omitted, the converted text content is returned directly." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Harmonize Markdown", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_txt",
            "description": "Convert Markdown to plain text by stripping all formatting — removes headers, bold/italic markers, links, images, code fences, and HTML tags. The result is a human-readable plain-text string with no markup.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to Plain Text", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_rtf",
            "description": "Convert Markdown to Rich Text Format (RTF). Produces an RTF document string preserving basic formatting: bold, italic, headers, lists, and code blocks.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to RTF", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_latex",
            "description": "Convert Markdown to LaTeX source code. Produces a LaTeX document fragment with \\section, \\textbf, \\textit, list environments, verbatim code blocks, and table environments. KaTeX math expressions are passed through as native LaTeX math.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to LaTeX", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_docx",
            "description": "Convert Markdown to a Microsoft Word DOCX file. Produces a binary .docx document with styled headings, bold/italic text, numbered and bulleted lists, and code formatting. Binary format — output_path should almost always be provided.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the binary .docx file." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to DOCX", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_pdf",
            "description": "Convert Markdown to a PDF document. Renders the Markdown as styled HTML (GFM tables, KaTeX math) and prints it to PDF via a headless Chromium browser. Binary format — output_path should almost always be provided.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the binary .pdf file." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to PDF", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_image",
            "description": "Convert Markdown to a PNG image. Renders the Markdown as styled HTML (GFM tables, KaTeX math) and takes a full-page screenshot via a headless Chromium browser. Binary format — output_path should almost always be provided.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the binary .png file." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to PNG Image", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_csv",
            "description": "Extract tables from Markdown and convert them to CSV format. Parses GFM pipe-tables from the input and outputs comma-separated values. Non-table content is ignored.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text containing tables." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to CSV", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_json",
            "description": "Convert Markdown to a structured JSON representation. Parses the document into a JSON object with sections, headings, paragraphs, lists, code blocks, and tables.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "title": { "type": "string", "description": "Optional. A document title string. Defaults to 'document' if omitted." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to JSON", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_xml",
            "description": "Convert Markdown to an XML document. Produces well-formed XML with an <?xml?> declaration and structured elements including section, heading, paragraph, list, code, and table.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "title": { "type": "string", "description": "Optional. The root XML element name and document title. Defaults to 'document' if omitted." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to XML", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_xlsx",
            "description": "Convert Markdown tables to a Microsoft Excel XLSX spreadsheet. Each table becomes a sheet in the workbook. Non-table content is ignored. Binary format — output_path should almost always be provided.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text containing tables." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the binary .xlsx file." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to XLSX", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_html",
            "description": "Convert Markdown to a complete, styled HTML document. Renders GFM (tables, task lists, strikethrough) and KaTeX math into semantic HTML with an embedded stylesheet.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to HTML", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_md",
            "description": "Export Markdown content, optionally harmonizing its formatting first. When harmonize=false (default), returns the input unchanged. When harmonize=true, applies normalization.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to export." },
                    "harmonize": { "type": "boolean", "description": "Optional. When true, normalizes Markdown syntax before returning or saving. Defaults to false." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Export Markdown", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "generate_html",
            "description": "Generate a complete, self-contained HTML document from Markdown with all styles inlined. Returns the HTML string directly — no file is written to disk. This tool is read-only and performs no file I/O.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "title": { "type": "string", "description": "Optional. Sets the <title> tag in the HTML document's <head> section. Defaults to 'Document' if omitted." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Generate HTML Document", "readOnlyHint": true, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_slack",
            "description": "Convert Markdown to Slack mrkdwn format. Bold as *, links as <url|text>, headers as bold text.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to Slack mrkdwn", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_discord",
            "description": "Convert Markdown to Discord-compatible format with styled bold/underline headers and preserved code blocks.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to Discord Markdown", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_jira",
            "description": "Convert Markdown to JIRA wiki markup with h1./h2. headings, {code} blocks, and [text|url] links.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to JIRA Markup", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_confluence",
            "description": "Convert Markdown to Confluence wiki markup with {info}, {note} panels and {code:language=x} syntax.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to Confluence Markup", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_asciidoc",
            "description": "Convert Markdown to AsciiDoc format with = headers, ---- code blocks, url[text] links, and image:: directives.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to AsciiDoc", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_rst",
            "description": "Convert Markdown to reStructuredText (RST) format with underlined headers, .. code-block:: directives, and RST reference syntax.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to reStructuredText", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_mediawiki",
            "description": "Convert Markdown to MediaWiki markup with == headers, '''bold''', <syntaxhighlight> tags, and {| wikitable syntax.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to MediaWiki", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_bbcode",
            "description": "Convert Markdown to BBCode format with [b], [i], [code], [url], [img] tags for phpBB/vBulletin forums.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to BBCode", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_textile",
            "description": "Convert Markdown to Textile markup format for Redmine, Basecamp, and CMS platforms.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to Textile", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_orgmode",
            "description": "Convert Markdown to Emacs Org Mode format with * headers, #+BEGIN_SRC/#+END_SRC code, and [[url][text]] links.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to Org Mode", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "convert_to_email_html",
            "description": "Convert Markdown to email-optimized HTML with all styles inlined, compatible with Outlook, Gmail, and Apple Mail.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The raw Markdown source text to convert." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Convert to Email HTML", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "html_to_markdown",
            "description": "Convert HTML content (full document or fragment) back to Markdown. Handles headings, tables, lists, code blocks, links, images, and inline formatting.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "html": { "type": "string", "description": "The HTML content to convert to Markdown." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["html"]
            },
            "annotations": { "title": "Import HTML to Markdown", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "repair_markdown",
            "description": "Repair broken Markdown from LLM output or copy-paste. Fixes unclosed code fences, broken tables, stray emphasis markers, missing heading spaces, and broken links.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The potentially broken Markdown text to repair." },
                    "output_path": { "type": "string", "description": "Optional. File path to save the result." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Repair Broken Markdown", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "lint_markdown",
            "description": "Lint Markdown and report issues as a JSON array with line number, severity, rule name, message, and fixable flag.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The Markdown text to lint." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Lint Markdown", "readOnlyHint": true, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "extract_code_blocks",
            "description": "Extract all code blocks from Markdown. Returns JSON array with language, code content, and start/end line numbers.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The Markdown text to extract code blocks from." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Extract Code Blocks", "readOnlyHint": true, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "extract_links",
            "description": "Extract all links and images from Markdown. Returns JSON with text, URL, line number, and type (inline, reference, image, autolink).",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The Markdown text to extract links from." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Extract Links", "readOnlyHint": true, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "generate_toc",
            "description": "Generate a Markdown Table of Contents from headings with configurable max_depth (1-6) and indented links.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The Markdown text to generate a TOC from." },
                    "max_depth": { "type": "number", "description": "Maximum heading depth (1-6, default: 6)." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Generate Table of Contents", "readOnlyHint": true, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "analyze_document",
            "description": "Analyze Markdown and return comprehensive statistics: line/word/character/paragraph/sentence counts, heading/code/table/link/image counts, and estimated reading time.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The Markdown text to analyze." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Analyze Document Statistics", "readOnlyHint": true, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "extract_structure",
            "description": "Extract full document structure overview: statistics, heading outline, code block summary, and link summary.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "markdown": { "type": "string", "description": "The Markdown text to extract structure from." }
                },
                "required": ["markdown"]
            },
            "annotations": { "title": "Extract Document Structure", "readOnlyHint": true, "destructiveHint": false, "idempotentHint": true, "openWorldHint": false }
        },
        {
            "name": "batch_convert",
            "description": "Batch-convert multiple Markdown documents to one or more output formats in a single call. Accepts an array of items (each with markdown content and optional title) and an array of target format IDs. Supports 22 formats: txt, csv, json, xml, xlsx, latex, rtf, docx, pdf, html, md, email, slack, discord, jira, confluence, asciidoc, rst, mediawiki, bbcode, textile, orgmode. Errors are isolated per item+format so one failure does not stop the rest. When output_dir is provided, files are written to disk; otherwise binary formats are returned as base64 and text formats inline. Returns a summary (total_conversions, successful, failed) plus per-item results.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "items": {
                        "type": "array",
                        "description": "Array of documents to convert. Each item must have a 'markdown' string and an optional 'title' string.",
                        "items": {
                            "type": "object",
                            "properties": {
                                "markdown": { "type": "string", "description": "The Markdown source text to convert." },
                                "title": { "type": "string", "description": "Optional. Document title used in PDF metadata, HTML title, XML root element." }
                            },
                            "required": ["markdown"]
                        }
                    },
                    "formats": {
                        "type": "array",
                        "description": "Array of output format IDs to convert each item into. Valid values: txt, csv, json, xml, xlsx, latex, rtf, docx, pdf, html, md, email, slack, discord, jira, confluence, asciidoc, rst, mediawiki, bbcode, textile, orgmode.",
                        "items": { "type": "string" }
                    },
                    "output_dir": { "type": "string", "description": "Optional. Directory path where converted files will be saved. Created automatically if it does not exist. When omitted, results are returned inline." }
                },
                "required": ["items", "formats"]
            },
            "annotations": { "title": "Batch Convert Documents", "readOnlyHint": false, "destructiveHint": false, "idempotentHint": false, "openWorldHint": false }
        }
    ],
    "prompts": [
        {
            "name": "convert-document",
            "description": "Convert a Markdown document to a specified output format. Supports: PDF, DOCX, HTML, LaTeX, CSV, JSON, XML, XLSX, RTF, PNG, TXT, MD.",
            "arguments": [
                { "name": "format", "description": "Target output format: pdf, docx, html, latex, csv, json, xml, xlsx, rtf, png, txt, or md", "required": true },
                { "name": "markdown", "description": "The Markdown content to convert", "required": true }
            ]
        },
        {
            "name": "extract-tables",
            "description": "Extract all tables from a Markdown document and export them as CSV or XLSX spreadsheet format.",
            "arguments": [
                { "name": "format", "description": "Output format for tables: 'csv' for plain text or 'xlsx' for Excel spreadsheet", "required": true },
                { "name": "markdown", "description": "The Markdown content containing tables to extract", "required": true }
            ]
        },
        {
            "name": "format-for-sharing",
            "description": "Prepare a Markdown document for sharing by harmonizing formatting and converting to portable formats (PDF and HTML) with professional styling.",
            "arguments": [
                { "name": "markdown", "description": "The Markdown content to format for sharing", "required": true }
            ]
        },
        {
            "name": "analyze-and-repair",
            "description": "Analyze a Markdown document for issues, repair problems, and return both the lint report and repaired document.",
            "arguments": [
                { "name": "markdown", "description": "The Markdown content to analyze and repair", "required": true }
            ]
        },
        {
            "name": "convert-for-platform",
            "description": "Convert Markdown to a platform-specific format: Slack, Discord, JIRA, Confluence, AsciiDoc, RST, MediaWiki, BBCode, Textile, or Org Mode.",
            "arguments": [
                { "name": "platform", "description": "Target platform: slack, discord, jira, confluence, asciidoc, rst, mediawiki, bbcode, textile, or orgmode", "required": true },
                { "name": "markdown", "description": "The Markdown content to convert", "required": true }
            ]
        },
        {
            "name": "document-overview",
            "description": "Get a comprehensive overview of a Markdown document: statistics, structure, TOC, code blocks, and links.",
            "arguments": [
                { "name": "markdown", "description": "The Markdown content to analyze", "required": true }
            ]
        }
    ],
    "resources": [
        {
            "uri": "markdown-formatter://supported-formats",
            "name": "Supported Output Formats",
            "description": "Complete JSON list of all 22 output formats with tool names and types — txt, csv, json, xml, xlsx, latex, rtf, docx, pdf, html, md, email, slack, discord, jira, confluence, asciidoc, rst, mediawiki, bbcode, textile, orgmode",
            "mimeType": "application/json"
        },
        {
            "uri": "markdown-formatter://conversion-guide",
            "name": "Conversion Guide",
            "description": "Guide for choosing the right output format based on your use case",
            "mimeType": "text/plain"
        }
    ]
}
